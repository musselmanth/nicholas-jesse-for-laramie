import ContactClient from './ContactClient';

export const metadata = {
  title: 'Get in Touch',
  description: 'Get in touch with the campaign to ask questions, share your thoughts on Ward 1 priorities, or find out how to get involved with Nicholas Jesse for Laramie City Council.',
}

export default function ContactPage() {
  // This simply loads your existing client-side logic!
  return <ContactClient />;
}